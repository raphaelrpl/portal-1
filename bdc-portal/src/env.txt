(function (window) {
    window.__env = window.__env || {};

    // API url Geoserver
    window.__env.urlGeoserver = URL_GEOSERVER;
    // API url STAC
    window.__env.urlStac = URL_STAC;
    // API url STAC-Compose
    window.__env.urlStacCompose = URL_STAC_COMPOSE;
    // API url WTSS
    window.__env.urlWTSS = URL_WTSS;
    // API url Oauth
    window.__env.urlOauth = URL_OAUTH;
    // API url Oauth APP
    window.__env.urlOauthApp = URL_OAUTH_APP;
    // API url Soloist
    window.__env.urlSoloist = URL_SOLOIST;
    // API url Maestro
    window.__env.urlMaestro = URL_MAESTRO;
    // API url RC Maestro
    window.__env.urlRCMaestro = URL_RC_MAESTRO;
    // API url DataSearch - INPE
    window.__env.urlDataSearchINPE = URL_DATASEARCH_INPE;
    // URL APM SERVE
    window.__env.urlAPMServer = URL_APM_SERVER;

}(this));