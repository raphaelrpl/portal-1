(function (window) {
    window.__env = window.__env || {};

    // API url Geoserver
    window.__env.urlGeoserver = URL_GEOSERVER;
    // API url STAC
    window.__env.urlStac = URL_STAC;
    // API url STAC-Compose
    window.__env.urlStacCompose = URL_STAC_COMPOSE;
    // API url WTSS
    window.__env.urlWTSS = URL_WTSS;
    // API url Oauth
    window.__env.urlOauth = URL_OAUTH;

}(this));